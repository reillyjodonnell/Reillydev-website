import React from 'react'

export default function HireMeCard(props) {
    return (
        <div>
            <h3 style={props.darkMode ? { color: 'white' } : null}>Hire me</h3>

        </div>
    )
}
