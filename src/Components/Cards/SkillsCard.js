import React from 'react'

export default function Skills(props) {
    return (
        <div>
            <h3 style={props.darkMode ? { color: 'white' } : null}>
                Skills
            </h3>

        </div>
    )
}
